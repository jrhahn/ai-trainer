[project]
name = "ai-trainer-backend"
version = "0.1.0"
description = "Strava OAuth backend for AI Trainer"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.135.3",
    "httpx==0.28.1",
    "python-dotenv==1.2.2",
    "uvicorn[standard]==0.44.0",
]

