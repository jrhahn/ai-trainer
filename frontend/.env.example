# URL of the Python backend API.
# For local development, run the backend on port 8000.
VITE_BACKEND_URL=http://localhost:8000

# Optional Authelia portal URL. Used for logout links in production.
VITE_AUTHELIA_URL=
