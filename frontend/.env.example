# URL of the Python backend API.
# For local development, run the backend on port 8000.
VITE_BACKEND_URL=http://localhost:8000

# Optional Authelia portal URL. When set, the login page delegates sign-in to
# Authelia and the app attempts to mint an API token from the Authelia session.
VITE_AUTHELIA_URL=
